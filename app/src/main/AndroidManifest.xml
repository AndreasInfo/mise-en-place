<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".MiseEnPlace"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mise en Place"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".ui.list.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.detail.RecipeDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.list.MainActivity" />

        <activity
            android:name=".ui.edit.RecipeEditActivity"
            android:exported="false"
            android:parentActivityName=".ui.list.MainActivity" />

    </application>

</manifest>
